---
// Button system (supersedes the original PROJECT_BRIEF.md Section 3 "solid
// ink pill for primary actions" rule — see CLAUDE.md "Tech stack" for the
// documented revision). Two roles, same as before:
// - primary: the bracket-tag mono label (matching the site's existing
//   "[ scaffold ]" / "[ case study ]" bracket-label device), with a fill
//   that sweeps in on hover/focus. Used for the one or two real actions
//   per page (CV download, hero CTA).
// - secondary: unchanged — a quiet underline text link.
interface Props {
  href: string;
  variant?: 'primary' | 'secondary';
  icon?: 'download' | 'arrow';
  class?: string;
}

const { href, variant = 'primary', icon, class: className } = Astro.props;
---

<a href={href} class:list={['btn', `btn--${variant}`, className]}>
  {variant === 'primary' && (
    <span class="btn__bracket" aria-hidden="true">
      [
    </span>
  )}
  <span class="btn__label">
    <slot />
  </span>
  {icon === 'download' && (
    <svg class="btn__icon" viewBox="0 0 16 16" fill="none" aria-hidden="true">
      <path
        d="M8 2.5V10M8 10L4.75 6.75M8 10L11.25 6.75"
        stroke="currentColor"
        stroke-width="1.3"
        stroke-linecap="round"
        stroke-linejoin="round"
      />
      <path d="M3 13H13" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" />
    </svg>
  )}
  {icon === 'arrow' && (
    <svg class="btn__icon" viewBox="0 0 16 16" fill="none" aria-hidden="true">
      <path
        d="M3 8H13M13 8L9.25 4.25M13 8L9.25 11.75"
        stroke="currentColor"
        stroke-width="1.3"
        stroke-linecap="round"
        stroke-linejoin="round"
      />
    </svg>
  )}
  {variant === 'primary' && (
    <span class="btn__bracket" aria-hidden="true">
      ]
    </span>
  )}
</a>

<style>
  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.4em;
    font-weight: 500;
    transition: color var(--motion-duration) var(--motion-easing);
  }

  .btn:focus-visible {
    outline: 2px solid var(--color-accent);
    outline-offset: 3px;
  }

  /* Primary: bracket-tag mono label with a fill that sweeps in behind it. */
  .btn--primary {
    position: relative;
    z-index: 0;
    overflow: hidden;
    font-family: var(--font-mono);
    font-size: var(--text-label);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
    color: var(--color-accent);
    border: var(--border-hairline) solid var(--color-accent);
    border-radius: 0;
    padding: 0.7em 0.9em;
  }

  .btn--primary > * {
    position: relative;
    z-index: 1;
  }

  .btn--primary::before {
    content: '';
    position: absolute;
    inset: 0;
    background: var(--color-accent);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform var(--motion-duration-slow) var(--motion-easing);
  }

  .btn--primary:hover,
  .btn--primary:focus-visible {
    color: var(--color-bg-primary);
  }

  .btn--primary:hover::before,
  .btn--primary:focus-visible::before {
    transform: scaleX(1);
  }

  .btn__icon {
    width: 0.9em;
    height: 0.9em;
  }

  /* Secondary: quiet underline text link — unchanged. */
  .btn--secondary {
    padding-block: 0.2em;
    font-family: var(--font-body);
    font-size: var(--text-nav);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
    border-bottom: var(--border-hairline) solid currentColor;
    color: var(--color-ink);
  }

  .btn--secondary:hover {
    color: var(--color-accent);
  }
</style>
