# components/layout

Structural chrome shared across pages: navigation, footer. These wrap
`src/layouts/BaseLayout.astro` output, not page content.

Built so far: `Header.astro` (nav items per PROJECT_BRIEF.md Section 4),
`Footer.astro` (dark-field, pairs with the homepage's ContactCTA section).
