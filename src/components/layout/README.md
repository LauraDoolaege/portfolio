# components/layout

Structural chrome shared across pages: navigation, footer. These wrap
`src/layouts/BaseLayout.astro` output, not page content.

Nothing has been built here yet — the nav and footer are page-adjacent
content (real nav items, footer links) that hasn't been approved/finalized
per PROJECT_BRIEF.md Section 4, so they aren't scaffolded as empty shells.
