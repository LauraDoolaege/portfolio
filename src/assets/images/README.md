# assets/images

Project photography (documentary/process shots, still-life hero-object
shots — the two locked imagery modes, PROJECT_BRIEF.md Section 3) and any
other images that should go through Astro's build-time image pipeline
(`astro:assets`), imported from `.astro`/`.ts` files.

No real photography exists yet (PROJECT_BRIEF.md Section 6). Per the
brief, use flat gray placeholder divs in markup until real assets are
supplied — don't add stock or generated photography here as a stand-in.
